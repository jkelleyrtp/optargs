fn main() {
    // let next_reddit_post = go_gme!(
    //     to_the_moon,
    //     rocket_ships: 10,
    // );
}

// #[optargs]
fn go_gme(
    to_the_moon: Option<bool>,
    rocket_ships: Option<usize>,
    doges: Option<usize>,
    diamond_hands: Option<&str>,
    tendies: Option<bool>,
) -> String {
    "blah".to_string()
}
